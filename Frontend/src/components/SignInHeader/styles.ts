import styled from "styled-components"

export const SingInHeaderContainer = styled.header`
  position: relative;
`

export const SignInBanner = styled.div`
  position: fixed;
  top: 3.2rem;
  left: 3.2rem;

  display: flex;
  align-items: center;
  gap: 1.8rem;

  img {
    width: 4rem;
    height: 4rem;
  }

  span {
    font-size: 3.2rem;
  }
`
